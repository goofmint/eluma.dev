name = "eluma-web"
main = "./workers/app.ts"
compatibility_date = "2024-12-18"
compatibility_flags = ["nodejs_compat"]

assets = { directory = "./build/client" }

[vars]
ENVIRONMENT = "production"

[env.preview]
name = "eluma-web-preview"
vars = { ENVIRONMENT = "preview" }
